apiVersion: v1
kind: Service
metadata:
  name: {{ include "hello-app.fullname" . }}-lb
  labels:
    {{- include "hello-app.labels" . | nindent 4 }}
spec:
  type: LoadBalancer
  selector:
    {{- include "hello-app.selectorLabels" . | nindent 4 }}
  ports:
    - port: 80
      targetPort: 80
